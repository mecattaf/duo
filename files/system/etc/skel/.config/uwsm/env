export XDG_SESSION_TYPE=wayland
export XDG_CURRENT_DESKTOP=scroll
export XDG_SESSION_DESKTOP=scroll
export QT_QPA_PLATFORMTHEME=qt5ct
export QT_QPA_PLATFORM=wayland
export MOZ_ENABLE_WAYLAND=1
# - MOZ_DBUS_REMOTE=1 (Firefox desktop integration)
# - MOZ_USE_XINPUT2=1 (Firefox touch support)

# Import variables into system-user environment
#exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP 
#exec hash dbus-update-activation-environment 2>/dev/null && \
#        dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP=sway 

## from environment.d
#GDK_BACKEND=wayland
#XDG_SESSION_TYPE=wayland
#XDG_CURRENT_DESKTOP=sway
#SDL_VIDEO_DRIVER=wayland
#QT_QPA_PLATFORMTHEME=qt5ct
#QT_AUTO_SCREEN_SCALE_FACTOR=1
#QT_QPA_PLATFORM=wayland
#QT_WAYLAND_DISABLE_WINDOWDECORATION=1

#UWSM_FINALIZE_VARNAMES: "Optional vars are taken by name from arguments and UWSM_FINALIZE_VARNAMES variable" uwsm(1) — Arch manual pages
#UWSM_WAIT_VARNAMES: "waits for WAYLAND_DISPLAY and all vars mentioned in UWSM_WAIT_VARNAMES" uwsm(1) — Arch manual pages
#UWSM_UNIT_RUNG: "Generated drop-in files can be managed in $XDG_CONFIG_HOME systemd configuration rung...UWSM_UNIT_RUNG=home var" GitHub - Vladimir-csp/uwsm: Universal Wayland Session Manager
# UWSM_FINALIZE_VARNAMES="SWAYSOCK"
#UWSM_APP_UNIT_TYPE: "Unit type of launched apps can be controlled by...setting its default via UWSM_APP_UNIT_TYPE env var" uwsm(1) — Arch manual pages
# export UWSM_FINALIZE_VARNAMES="SWAYSOCK"  # Only if apps need SWAYSOCK
